# Dataset Instructions

Please place your downloaded datasets in this directory.

It is recommended to use the following structure:
- `datasets/kaggle/` for the MVTec AD primary dataset
- `datasets/roboflow/` for the Roboflow defect dataset

The Python machine learning pipeline scripts (`ml/dataset_utils.py` and `ml/train.py`) will look for the datasets in these folders.
